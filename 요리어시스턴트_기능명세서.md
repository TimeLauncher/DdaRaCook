# 기술·기능명세서 — 간헐적 1인칭 비전 기반 요리 단계 추적 어시스턴트 (가제)

문서 버전 v2 · 3인 팀 프로젝트 · 개발 기간 3주

---

## 1. 개요

### 1.1 한 줄 정의

> 레시피를 등록하면 단계별 음성 안내를 제공하고, 시각 판정이 필요한 단계에서 안경 카메라를 **간헐적으로 작동**시켜 시작 상태와 현재 상태를 비교하고 실제 단계 완료를 판단하는 서비스.

### 1.2 기존과 무엇이 다른가

| | 기존 요리 앱 · 레시피 영상 | 스톡 Meta AI | 본 서비스 |
|---|---|---|---|
| 단계 진행 | 사용자가 화면 터치 | 사용자가 음성 요청 | **카메라 판정에 따라 자동** |
| 내 상태 인식 | 없음 | 일회성 질문 | **시작 상태와 현재 상태를 비교** |
| 손 사용 | 필요 | 불필요 | 불필요 |
| 카메라 사용 | 없음 | 질문 시점 | **단계별 짧은 검사 구간만 사용** |

**근거**: Meta는 요리를 AI 글래스의 대표 유즈케이스로 홍보하고 있으나, Connect 라이브 데모에서 AI가 아직 섞지도 않은 재료를 "이미 섞으셨으니 다음 단계로"라고 안내하는 실패가 발생했다. 대본을 따라 읽을 뿐 **사용자가 실제로 어느 단계에 있는지를 보지 않기 때문**이다.

본 서비스의 존재 이유는 이 지점 하나다. 단순히 레시피를 읽어주는 것이 아니라 **정해진 시간이 아니라 실제로 보이는 변화에 따라 다음 단계로 전환**한다.

### 1.3 핵심 기술 가설

> 디스플레이가 없는 스마트글라스에서도 연속 촬영 없이 간헐적으로 수집한 1인칭 시각 정보만으로 실제 작업 단계의 완료 시점을 신뢰성 있게 판단할 수 있는가?

요리는 이 가설을 검증하기 위한 대표 시나리오다. 같은 구조는 향후 조립·실험·정비 등 절차 기반 작업으로 확장할 수 있다.

### 1.4 대상

- 레시피를 보면서 요리하는 일반 사용자
- 손에 물·기름이 묻어 화면을 만지기 어려운 상황
- 조리 시간 20~40분, 단계 5~10개 수준의 요리

---

## 2. 하드웨어 제약 (확인된 사실)

설계는 아래 제약 위에서 이루어진다. 이를 위반하는 기능은 명세에 포함하지 않는다.

| 항목 | 사실 | 설계 영향 |
|---|---|---|
| 디스플레이 | Gen 2에 없음 | 출력은 음성 전용 |
| 탭 제스처 | 세션 제어(일시정지/재개/종료)에 예약 | **촬영 버튼으로 사용 불가 → 자동 판정 필수** |
| 커스텀 호출어 | "Hey Meta" 연동 미지원 | 자체 STT로 처리 |
| 스트림 해상도 | HIGH 720×1280 / MEDIUM 504×896 / LOW 360×640 | 미세 판정 불리 |
| 프레임레이트 | 2 / 7 / 15 / 24 / 30 FPS | **2 FPS 사용** |
| 화질 | 블루투스 대역폭 기반 적응형 압축. 낮은 해상도·프레임레이트일수록 압축 손실 적음 | 저FPS가 유리 |
| 스트림 생명주기 | `start()` / `stop()` 지원 | 세션은 유지하고 검사 시점에만 짧게 스트리밍 |
| 사진 캡처 | 스트림 활성 중 `capturePhoto()` | 판정 시점에만 호출 |
| 장시간 스트림 | Android에서 1분 이상 스트림 중 여러 캡처 시 간헐적 충돌 가능 | **전체 조리 시간 연속 스트리밍 제외** |
| 글래스 마이크 | HFP 8kHz 모노, 빔포밍으로 **착용자 음성만 격리** | 주변음 녹음 불가, 명령어에는 적합 |
| A2DP ↔ HFP | **동시 사용 불가.** HFP 켜면 출력도 8kHz | MVP는 폰 마이크 사용 |
| 세션 | 기기당 1개. 개발자 모드에서 서드파티 앱 1개만 등록 유지 | **기기 담당자 1명 고정** |
| 앱 실행 위치 | 안경이 아닌 폰 | 로컬 DB·서버 API 호출 자유 |

---

## 3. 시스템 구조

```
[안경]                    [폰 앱]                      [서버/API]
카메라 ◀──검사 시 start/평소 stop── 검사 스케줄러
카메라 ──2~3초·2FPS 스트림──▶ 프레임 버퍼
                        ├─ 선명도 검사 → 대표 프레임 선택
                        ├─ capturePhoto()
                        └─ 시작/현재 이미지 ────────▶ VLM 판정 서버
                                                      (이미지 + 단계 컨텍스트)
                        ◀──────────────────────────── DONE/NOT_DONE/CANNOT_TELL
                        ├─ 단계 상태 갱신
                        ├─ 다음 검사 예약 또는 자동 진행
                        ├─ 스트림 stop
                        └─ TTS 생성
스피커 ◀──A2DP────────── 음성 안내

마이크(폰) ─────────────▶ STT → 음성 명령 처리
```

### 3.1 핵심 원칙

> 시간은 완료 조건이 아니라 **언제 카메라 검사를 시작할지 정하는 기준**이다. 단계 완료 여부는 시각 판정으로 결정한다.

예를 들어 레시피에 "양파를 5분 볶기"라고 적혀 있어도 5분을 기다린 뒤 자동 완료하지 않는다. 2분부터 짧은 시각 검사를 시작하고 20초마다 재검사하여, 실제 변화가 충분하면 5분 이전에도 다음 단계로 진행한다.

---

## 4. 기능 명세

### F1. 레시피 등록

| ID | 기능 | 설명 | 우선순위 |
|---|---|---|---|
| F1-1 | 레시피 직접 입력 | 제목, 재료 목록, 단계 목록 입력 | 필수 |
| F1-2 | 단계별 판정 조건 입력 | 각 단계에 "무엇이 보이면 완료인지" 텍스트로 정의 | 필수 |
| F1-3 | 단계별 검사 정책 | 최초 검사 시점, 재검사 간격, 연속 완료 기준 | 필수 |
| F1-4 | 판정 유형 선택 | 아래 §5.1의 유형 중 선택 | 필수 |
| F1-5 | 대표 샘플 레시피 1종 내장 | 4~5단계, A등급 중심으로 시연·테스트 | 필수 |
| F1-6 | 영상 URL → 레시피 자동 변환 | 자막·프레임 분석해 단계 구조화 | **제외 (확장)** |
| F1-7 | 단계별 병렬 타이머 정의 | 그 단계에서 켜고 **이후 단계에서도 계속 도는** 시계. 이름·지속 시간·만료 안내 문구 | 필수 |

**F1-7이 필요한 이유**

면을 삶는 8분은 손질·볶기와 **동시에** 흐른다. 단계 경과 시간(`stepElapsedSeconds`)은 단계가 바뀌면
리셋되므로 이것으로 표현할 수 없다. 한 단계에 묶어두면 사용자는 냄비 앞에서 8분을 서 있어야 한다.

```
단계 1: 끓는 물에 스파게티면을 넣고 8분 타이머를 건다
  판정 유형: 시간 전용 (자동 판정 없음)
  병렬 타이머: "면 삶기" · 480초 · "면 8분이 다 됐어요. 면을 건져 두세요."
```

만료 시각은 세션에 **절대 시각**으로 남는다. 앱을 껐다 켜도 냄비는 계속 끓기 때문이다(F2-5와 같은 이유).
같은 단계를 다시 시작하면(F5-4 "이전") 냄비를 다시 앉히는 것이므로 시계도 다시 건다.

**F1-2 예시**

```
단계 3: 기름을 두르고 야채를 중약불로 볶기
  판정 유형: 색상 변화
  완료 조건: "팬 안의 양파가 흰색/불투명에서 반투명하게 변했는가"
  최초 검사: 단계 시작 2분 후
  재검사 간격: 20초
  연속 DONE 기준: 2회
```

### F2. 세션 관리

| ID | 기능 | 설명 | 우선순위 |
|---|---|---|---|
| F2-1 | 레시피 선택 후 세션 시작 | 폰에서 시작 버튼. **조리 전이므로 손이 깨끗함** | 필수 |
| F2-2 | 안경 연결 상태 표시 | 미연결 시 안내 | 필수 |
| F2-3 | 세션 일시정지/재개 | 안경 탭 또는 폰 | 필수 |
| F2-4 | 세션 종료 | 마지막 단계 완료 또는 수동 종료 | 필수 |
| F2-5 | 세션 중단 복구 | 안경 벗음·힌지 접힘으로 끊긴 경우 재개 | 권장 |

**F2-5 주의**: SDK 문서상 힌지를 접으면 블루투스가 끊기고 세션이 `STOPPED`가 된다. 다시 펴도 세션은 자동 복구되지 않으므로 새 세션을 시작해야 한다. 이때 **진행 중이던 단계 번호를 유지**해야 한다.

### F3. 간헐적 스트림 및 검사 프레임 선별

| ID | 기능 | 설명 | 우선순위 |
|---|---|---|---|
| F3-1 | 단계 시작 이미지 확보 | 변화 비교가 필요한 단계의 기준 이미지 저장 | 필수 |
| F3-2 | 최초 검사 예약 | `earliestCheckSeconds` 이후 자동 검사 | 필수 |
| F3-3 | HIGH·2FPS 스트림 시작 | 검사 시점에만 2~3초 실행 | 필수 |
| F3-4 | 대표 프레임 선별 | 블러가 적고 조리 대상이 보이는 프레임 선택 | 필수 |
| F3-5 | `capturePhoto()` 호출 | 스트림 활성 상태에서 판정용 사진 확보 | 필수 |
| F3-6 | 스트림 즉시 종료 | 캡처 후 `stream.stop()`으로 카메라 사용률 제한 | 필수 |
| F3-7 | 재검사 예약 | 미완료·판정 불가 시 지정 간격 후 재검사 | 필수 |
| F3-8 | 사용자 즉시 검사 | "확인해줘" 명령 시 예약 전이라도 검사 | 필수 |

**설계 의도**: 매 프레임을 서버로 보내거나 20~40분 동안 스트림을 유지하지 않는다. 단계별로 필요한 시점에만 카메라를 짧게 켜서 발열·배터리·비용을 제한하면서도 실제 상태를 기준으로 진행한다.

**검사 정책 파라미터** (실측 후 확정)

```
최초 검사 시점: 단계 시작 후 120초
재검사 간격: 20초
카메라 버스트: 3초
연속 DONE 기준: A등급 1회 / B등급 2회
```

### F4. 단계 판정

| ID | 기능 | 설명 | 우선순위 |
|---|---|---|---|
| F4-1 | 이미지 + 레시피 컨텍스트로 VLM 호출 | | 필수 |
| F4-2 | 3값 응답 강제 | `DONE` / `NOT_DONE` / `CANNOT_TELL` | 필수 |
| F4-3 | 연속 DONE 누적 | 유형별 기준 횟수를 충족하면 다음 단계 자동 진행 | 필수 |
| F4-4 | CANNOT_TELL 시 사용자 안내 | "잘 안 보여요. 냄비를 한 번 봐주세요" | 필수 |
| F4-5 | 연속 CANNOT_TELL 처리 | 3회 연속 시 판정 중단, 수동 모드 전환 | 필수 |
| F4-6 | 장시간 미완료 안내 | 최대 예상 시간을 넘겨도 자동 완료하지 않고 사용자에게 안내 | 필수 |
| F4-7 | 이전 프레임 비교 판정 | 단계 시작 시점 이미지와 함께 전송 | 필수 |

**F4-7이 정확도의 핵심.** 사진 한 장으로 "다 익었나"를 묻는 것보다, **단계 시작 시점과 현재를 함께 주고 "변화가 충분한가"를 묻는 편**이 판정이 명확해진다.

### F5. 음성 명령 (사용자 개입)

| ID | 명령 | 동작 | 우선순위 |
|---|---|---|---|
| F5-1 | "다음" | 판정 무시하고 다음 단계로 | 필수 |
| F5-2 | "아직" | 현재 단계 유지, 판정 쿨다운 연장 | 필수 |
| F5-3 | "다시" | 현재 단계 안내 재생 | 필수 |
| F5-4 | "이전" | 이전 단계로 되돌림 | 필수 |
| F5-5 | "확인해줘" | 예약 시각 전이라도 즉시 시각 판정 | 필수 |
| F5-6 | "재료 뭐야" | 현재 단계 재료 안내 | 권장 |
| F5-7 | "이거 맞아?" | 즉시 캡처 후 재료 대조 | 권장 |
| F5-8 | "몇 단계야" | 현재 단계 안내 | 권장 |

**자동 판정은 틀릴 수 있다. 사용자가 항상 덮어쓸 수 있어야 한다.** F5-1, F5-2가 없으면 잘못된 판정에서 벗어날 방법이 없다.

**입력 경로**: MVP는 **폰 마이크**를 사용한다. 글래스 마이크(HFP)를 쓰면 A2DP가 끊겨 음성 안내 품질이 8kHz로 떨어지고, 카메라 스트림과의 라우팅 순서 제약이 추가된다. 폰은 조리대에 놓인 상태이므로 짧은 명령어 인식에 문제가 없다고 가정하며, 실측으로 검증한다.

### F6. 음성 안내

| ID | 기능 | 설명 | 우선순위 |
|---|---|---|---|
| F6-1 | 단계 시작 안내 | "3단계. 야채를 중약불로 볶으세요" | 필수 |
| F6-2 | 단계 완료 안내 | "야채 다 볶였네요. 다음 단계로 갈게요" | 필수 |
| F6-3 | 판정 불가 안내 | F4-4 | 필수 |
| F6-4 | 검사 시점 안내 | "상태를 확인할게요. 팬을 봐주세요" | 필수 |
| F6-5 | 타이머 안내 | 시간 기반 단계는 타이머로 알림 | 필수 |
| F6-6 | 안내 길이 제한 | 한 번에 2문장 이내 | 필수 |
| F6-7 | 병렬 타이머 만료 안내 | F1-7의 타이머가 끝나면 **그때 진행 중인 단계 위로** 안내. 1회만 | 필수 |

**F6-6 이유**: 조리 중에는 긴 음성을 따라가기 어렵고, 되감기가 불가능하다.

**F6-7 주의**: 타이머 감시는 1초마다 돌므로 발화 여부를 세션에 기록해 반복을 막는다.
세션이 끝난 뒤(`SESSION_COMPLETED`)에는 울리지 않는다. 조리 화면에는 남은 시간을 함께 표시한다.

### F7. 세션 기록

| ID | 기능 | 설명 | 우선순위 |
|---|---|---|---|
| F7-1 | 단계별 완료 시각 저장 | | 필수 |
| F7-2 | 판정 시 캡처 이미지 저장 | | 필수 |
| F7-3 | 판정 결과 로그 | DONE/NOT_DONE/CANNOT_TELL, 응답 지연 | 필수 |
| F7-4 | 세션 요약 화면 | 총 소요시간, 단계별 시간, 사진 | 권장 |
| F7-5 | 수동 개입 횟수 기록 | **정확도 평가 지표** | 필수 |
| F7-6 | 테스트 정답 라벨 | 실제 완료 여부를 별도 기록해 오탐·미탐 계산 | 필수 |
| F7-7 | 카메라 사용 시간 | 전체 세션 대비 스트림 활성 비율 계산 | 필수 |

수동 개입 횟수만으로는 정확도를 계산할 수 없다. 사용자가 오판정을 발견하지 못했을 수도 있으므로 테스트에서는 실제 정답 라벨을 별도로 기록해 **잘못된 단계 진행률, 완료 감지율, CANNOT_TELL 비율**을 계산한다.

---

## 5. 판정 설계

### 5.1 판정 유형과 난이도

| 등급 | 유형 | 예시 | 신뢰도 | MVP |
|---|---|---|---|---|
| **A** | 존재 여부 | 재료가 팬에 들어갔는가 | 높음 | **포함** |
| **A** | 재료 식별 | 지금 든 게 양파인가 | 높음 | **포함** |
| **B** | 개수 | 큰 재료 2개가 들어갔는가 | 중간 | 선택 |
| **B** | 큰 색상 변화 | 양파가 반투명하게 변했는가 | 중간 | 포함 |
| **B** | 상태 전환 | 물이 끓고 있는가 | 중간 | 선택 |
| **C** | 정도 판정 | 딱 알맞게 익었는가 | **낮음** | **제외** |
| **C** | 질감 판정 | 반죽이 충분히 치대졌는가 | **낮음** | **제외** |

**C 등급을 MVP에서 제외하는 이유**: 사진만으로는 사람도 판단이 어렵고, LLM이 **확신 있게 틀린 답을 내놓는다.** "네, 딱 좋습니다" 하고 태우는 것이 최악의 실패 모드다.

**A등급을 쓸 수 있으면 A등급을 쓴다. B등급 개수는 제한하지 않는다.**

원래는 "대표 레시피는 A등급 중심, B등급은 1개만"이었으나 **개수 규약을 걷어냈다.** 요리에 따라
확인할 가치가 있는 것이 애초에 색 변화뿐인 경우가 있다 — 고기가 익는 것도, 면에 양념이 배는 것도
"무엇이 있는가"가 아니라 "색이 변했는가"다. 존재 판정으로 우겨넣으면 재료를 넣는 즉시 참이 되어
조리 과정을 전혀 담지 못한다. **B등급 정확도는 아직 측정된 적이 없으므로, 실제로 해보고 안 되면
그때 A등급으로 되돌린다.** 측정 전에 개수부터 묶는 것은 근거 없는 제약이다.

C등급 제외만은 유지한다 — 아래 이유가 측정과 무관하게 성립한다.

### 5.1.1 대표 샘플 레시피의 유형 배치

F1-5의 대표 레시피는 **소세지야채볶음(쏘야)** 이다. 위 등급표가 실제로 어떻게 배치되는지 보기 위한 표이며, 촬영·라벨링과 레시피 픽스처(둘 다 3번)가 이 표를 정본으로 삼는다.

| # | 단계 | 판정 유형 | 등급 | 완료 조건 | 시작 이미지 | 연속 DONE |
|---|---|---|---|---|---|---|
| 1 | 야채를 자르고 소세지에 칼집을 낸다 | 상태 전환 | B | 도마 위에 통째로 남은 야채 덩어리가 없는가 | 없음 | 2회 |
| 2 | 양념장을 만든다 | 시간 전용 | — | (자동 판정 없음 · "다음" 명령으로 진행) | — | — |
| 3 | 기름을 두르고 야채를 중약불로 볶는다 | 색상 변화 | B | 시작 시점 사진과 비교해 팬 안의 양파가 조금이라도 더 반투명해졌는가 | **전송** | 2회 |
| 4 | 소세지를 넣고 2~3분간 볶는다 | 상태 전환 | B | 시작 시점 사진과 비교해 소세지 칼집이 벌어졌는가 | **전송** | 2회 |
| 5 | 양념장을 넣고 약불로 3분간 볶는다 | 시간 전용 | — | (자동 판정 없음 · 3분 타이머 + 과열 시 불 줄이기 안내) | — | — |

**완료 조건 문장의 정본은 이 표다 — 단, 대표 레시피에 한한다.** F1-2 예시와 §5.2는 여기서 인용한다.
그 밖의 레시피는 `RecipeFixtures.kt`가 정본이고, 판정 설계의 근거는 `docs/`에 남긴다.
레시피를 하나 늘릴 때마다 이 문서를 고치게 하면 명세서가 레시피 창고가 된다.

**1단계와 4단계는 같은 상태 전환인데 시작 이미지 정책이 반대다.** 1단계는 "덩어리가 없는가"(절대 판정), 4단계는 "시작 시점 사진과 비교해"(상대 판정)이기 때문이다. 판정 유형으로는 가를 수 없으므로 [`CONTRACT.md`](CONTRACT.md) §3.2를 **"완료 조건이 시작 시점 대비 변화를 묻는가"** 기준으로 개정했다(v1.3). 앱은 `RecipeStep.needsStartImage` 로 그 판단을 담는다.

**상대 판정 단계의 기준 사진은 단계 시작 15초 뒤에 찍는다.** 안내 직후에는 아직 재료가 팬에 들어가지 않아 빈 팬이 기준이 되기 때문이다.

> ⚠️ **최초 검사 시점이 런타임에서 무시된다.** 3·4단계 픽스처의 `earliestCheckSeconds`(각 2분)는
> `withAutomaticInspectionInterval()` 이 전 단계를 30초로 덮어써서 실제로는 적용되지 않는다.
> §3.1의 "그 시간 동안은 검사하지 않는다" 원칙이 지금 코드에서는 지켜지지 않는다. 별도 처리 필요.

> **서버가 판정하는 3단계가 전부 B등급이다.** 개수 규약을 걷어냈으므로(§5.1) 이대로 간다.
> 다만 **B등급 정확도는 측정된 적이 없다** — 보고된 85.7%는 A등급 숫자다. 2회차 촬영·평가에서
> 재고, 미달이면 4단계를 존재 판정("팬 안에 비엔나소세지가 들어 있는가", A등급)으로 되돌린다.
> 근거와 경위는 [`docs/contract-compliance.md`](docs/contract-compliance.md).

### 5.2 LLM 프롬프트 구조

문자열 정본은 [`server/prompts.py`](server/prompts.py)이며, 이 절은 구조 설명이다. 프롬프트를 고칠 때는 `PROMPT_VERSION`을 함께 올린다.

```
[시스템]
당신은 요리 중인 사람의 스마트 안경 카메라 사진을 보고
"지금 이 조리 단계가 끝났는가"만 판정하는 검사기입니다.

DONE / NOT_DONE / CANNOT_TELL 의 정의와 함께 다섯 규칙을 준다.
 1. 확신이 없으면 DONE 을 쓰지 마라 — 애매하면 NOT_DONE 또는 CANNOT_TELL
 2. 사진에 보이는 것만 근거로 삼아라. 경과 시간만으로 DONE 을 판정하지 마라
 3. 대상이 프레임에 없거나 가려졌으면 CANNOT_TELL / TARGET_NOT_VISIBLE
 4. 흔들리거나 초점이 나갔으면 CANNOT_TELL / BLURRY
 5. 이미지가 2장이면 "시작 대비 변화가 완료 조건을 만족하는가"로 판단하라

[사용자]
[조리 단계] 3단계: 기름을 두르고 야채를 중약불로 볶기
[완료 조건] 팬 안의 양파가 흰색/불투명에서 반투명하게 변했는가
[판정 유형] COLOR_CHANGE — 색이나 투명도의 변화를 보십시오. 조명 때문에
            달라 보이는 것과 실제 조리로 변한 것을 구분하십시오.
[경과 시간] 4분 20초 (260초)
[제공 이미지] 시작 시점 1장 + 현재 1장 (두 장을 비교하십시오)

이미지 1 — 단계를 시작한 시점:
이미지 2 — 현재:

위 완료 조건이 충족되었습니까? JSON 객체 하나만 출력하십시오.

[응답 형식]
{
  "verdict": "DONE | NOT_DONE | CANNOT_TELL",
  "reasonCode": "VISIBLE_CHANGE | NO_CHANGE | TARGET_NOT_VISIBLE | BLURRY | OTHER"
}
```

⚠️ `reasonCode`는 **다섯 종**이다. 앱은 모르는 값이 와도 크래시하지 않아야 하며, **미지의 값은 `OTHER`로 처리**한다 ([`CONTRACT.md`](CONTRACT.md) §4). 네트워크 실패를 뜻하는 값으로 떨어뜨리면 안 된다 — 판정 실패와 네트워크 실패의 카운터가 섞인다(§5.3 · `CONTRACT.md` §5).

📌 **레시피 이름은 모델에 전달하지 않는다.** 앱은 `recipeId`를 보내지만 프롬프트에는 넣지 않는다. "소세지야채볶음"이라는 맥락은 모델에게 "이쯤이면 볶였겠지"라는 **상식 추측의 근거**를 주는데, 그건 위 규칙 2가 금지한 것이고 곧장 오탐으로 이어진다. 넣는 편이 나은지는 3번이 A/B로 측정한다.

### 5.3 판정 실패 대응

```
CANNOT_TELL 1회  → "잘 안 보여요. 냄비 쪽을 봐주세요"
CANNOT_TELL 2회  → 15초 후 재시도
CANNOT_TELL 3회  → 자동 판정 중단, "다음이라고 말씀해주시면 넘어갈게요"
```

**자동 판정이 실패해도 요리는 계속 진행되어야 한다.** 수동 모드로 떨어지는 경로가 반드시 있어야 한다.

---

## 6. 데이터 모델

```kotlin
data class Recipe(
    val id: String,
    val title: String,
    val ingredients: List<Ingredient>,
    val steps: List<RecipeStep>
)

data class RecipeStep(
    val order: Int,
    val instruction: String,        // 사용자에게 읽어줄 내용
    val checkType: CheckType,       // A/B 등급 유형
    val checkCondition: String,     // LLM에 전달할 완료 조건
    val inspectionPolicy: InspectionPolicy,
    val targetIngredients: List<String>
)

data class InspectionPolicy(
    val earliestCheckSeconds: Int,       // 최초 자동 검사 시점
    val checkIntervalSeconds: Int,       // 미완료 시 재검사 간격
    val burstSeconds: Int,               // 한 번에 스트리밍할 시간
    val requiredConsecutiveDone: Int,    // 자동 진행에 필요한 연속 DONE
    val maxExpectedSeconds: Int          // 초과 안내 기준, 자동 완료 기준 아님
)

enum class CheckType {
    PRESENCE,       // 존재 여부
    COUNT,          // 개수
    IDENTIFY,       // 재료 식별
    COLOR_CHANGE,   // 색상 변화
    STATE_CHANGE,   // 상태 전환
    TIME_ONLY       // 판정 없이 시간만
}

enum class Verdict { DONE, NOT_DONE, CANNOT_TELL }

data class CookingSession(
    val id: String,
    val recipeId: String,
    val startedAt: Long,
    var currentStep: Int,
    val stepLogs: MutableList<StepLog>
)

data class StepLog(
    val stepOrder: Int,
    val startedAt: Long,
    var completedAt: Long?,
    val judgments: MutableList<JudgmentLog>,
    var manualOverride: Boolean = false   // F7-5 지표
)

data class JudgmentLog(
    val requestedAt: Long,
    val verdict: Verdict,
    val reasonCode: String,
    val latencyMs: Long,
    val streamDurationMs: Long,
    val imagePath: String,
    val groundTruth: Verdict? = null     // 테스트에서만 입력
)
```

---

## 7. MVP 범위

### 포함

- 최소 레시피 직접 입력 + 대표 샘플 레시피 1종(4~5단계)
- 세션 시작·종료 (폰)
- 검사 시점마다 HIGH·2FPS 스트림을 2~3초 실행 후 종료
- 단계 시작 이미지와 현재 이미지 비교
- A등급 중심 판정 + B등급 대표 단계 1개
- 음성 명령 5종 (다음/아직/다시/이전/확인해줘)
- 음성 안내
- Room/SQLite 기반 로컬 세션 기록
- VLM 호출용 얇은 판정 서버
- 오탐·미탐·지연·카메라 사용률 측정

### 제외

- 전체 조리 시간 연속 스트리밍
- 픽셀 변화량 기반 자동 안정 프레임 감지
- 영상 URL → 레시피 자동 변환
- C 등급 정도 판정
- 글래스 마이크(HFP) 사용
- 다중 레시피 동시 진행
- 사용자 계정·클라우드 동기화
- 이미지 서버 영구 저장
- 파인튜닝

### MVP 성공 정의

> 대표 레시피 한 개에서 `단계 안내 → 자동 검사 예약 → 짧은 스트림 → 시작/현재 이미지 판정 → 다음 단계 진행 → 로그 저장`이 실기기에서 끝까지 동작하고, 잘못된 자동 진행이 없는 시연을 완성한다.

---

## 8. 서버 범위

### 8.1 결론

**작은 서버는 필요하지만 서버 DB는 필요 없다.** 모바일 앱에서 VLM을 직접 호출하면 API 키가 앱에 노출될 수 있으므로, 판정 요청만 중계하는 단일 API를 둔다.

```text
POST /judge-step

입력
- 현재 단계와 완료 조건
- 경과 시간
- 단계 시작 이미지
- 현재 이미지

출력
- verdict: DONE | NOT_DONE | CANNOT_TELL
- reasonCode
- latencyMs
```

### 8.2 실행 위치

| 폰 앱 | 얇은 서버 |
|---|---|
| 안경 연결·스트림·캡처 | VLM API 키 보관 |
| 레시피와 현재 단계 상태 | VLM 호출 및 프롬프트 버전 관리 |
| 검사 스케줄러·타이머 | 응답 JSON 스키마 검증 |
| STT/TTS | 모델 응답 지연 기록 |
| Room/SQLite 로그 | 이미지는 판정 후 즉시 폐기 |

서버는 단일 서버리스 함수 수준이면 충분하다. 사용자 계정, 클라우드 DB, 관리자 페이지는 만들지 않는다.

---

## 9. 3명 역할 분담

| 담당 | 주 책임 | 세부 업무 | 주요 산출물 |
|---|---|---|---|
| **1. 기기·카메라 담당** | Meta DAT 파이프라인 | 등록, 세션 생명주기, 스트림 start/stop, 2FPS 프레임 수신, `capturePhoto()`, 대표 프레임 선별, 실기기 디버깅 | `CaptureResult(imageUri, timestamp, streamDurationMs)` |
| **2. 앱·상태·음성 담당** | Android 제품 흐름 | 레시피 화면, CookingSession 상태 머신, 검사 스케줄러, 타이머, 폰 STT, TTS, Room 로그, 중단 복구 | 단계 진행이 가능한 앱 UI와 상태 엔진 |
| **3. AI·서버·평가 담당** | 판정 품질과 검증 | `/judge-step`, 프롬프트·JSON 검증, 이미지 비교 판정, 테스트 데이터 라벨링, 정확도·지연·오탐 평가 | `JudgmentResult` API와 평가 리포트 |

### 9.1 협업 규칙

- 실기기 등록과 통합 빌드는 **1번 담당자가 고정 관리**한다.
- 2번을 기기 담당 백업으로 지정해 지식이 한 명에게만 묶이지 않게 한다.
- 2·3번은 실제 안경이 없어도 MockDeviceKit, 폰 카메라, 저장된 테스트 이미지로 개발한다.
- 첫날 아래 두 데이터 계약을 확정하고 이후 임의로 바꾸지 않는다.

```kotlin
data class CaptureResult(
    val imageUri: String,
    val capturedAt: Long,
    val streamDurationMs: Long
)

data class JudgmentResult(
    val verdict: Verdict,
    val reasonCode: String,
    val latencyMs: Long
)
```

---

## 10. 개발 순서

AI 글래스 경험이 없으므로 처음부터 전체 앱을 만들지 않는다. **공식 CameraAccess 샘플을 실기기에서 실행한 뒤 한 기능씩 붙인다.**

### W0 · 착수 직후 2일 — 생존 실험

| 순서 | 실험 | 통과 기준 | 실패 시 |
|---|---|---|---|
| 1 | 공식 샘플로 연결·프리뷰·캡처 | 실제 안경 영상과 사진 수신 | SDK·펌웨어·등록부터 해결 |
| 2 | `start → 3초 → capture → stop` 20회 | 19회 이상 성공, 치명적 발열 없음 | 한 단계 동안만 스트림 유지 방식으로 변경 |
| 3 | 실제 DAT 이미지 20쌍 VLM 판정 | A등급 80% 이상, 확신 있는 오답 0회 | A등급 더 축소 또는 수동 확인형으로 변경 |
| 4 | 캡처→서버→음성 왕복 | 목표 3초 이내 | 이미지 축소, 프롬프트·모델 최적화 |
| 5 | 폰 마이크 명령 20회 | 90% 이상 인식 | 큰 폰 버튼 fallback 추가 |

### W1 · 파이프라인 관통

목표는 정확도가 아니라 **한 바퀴가 도는 것**이다.

```text
폰에서 테스트 버튼
→ 안경 스트림 시작
→ 사진 캡처
→ /judge-step 호출
→ DONE/NOT_DONE/CANNOT_TELL 수신
→ 안경 스피커로 결과 재생
→ 스트림 종료
```

- 1번: 실제 안경에서 캡처 결과를 앱에 전달
- 2번: 한 단계짜리 상태 화면과 TTS 연결
- 3번: 가짜 이미지로도 호출 가능한 판정 API 완성
- W1 완료 조건: 세 사람이 만든 모듈이 하나의 앱에서 연결됨

### W2 · 간헐적 판정과 상태 관리

- `InspectionPolicy` 기반 최초 검사·재검사 예약
- 단계 시작 이미지와 현재 이미지 비교
- 연속 DONE, CANNOT_TELL 3회, 수동 override 구현
- "확인해줘/다음/아직/다시/이전" 명령
- Room 로그와 네트워크 실패 fallback
- 대표 레시피의 A등급 단계부터 연결한 후 B등급 1개 추가

### W3 · 정확도·사용성·발표

- 대표 레시피 전체 10회 반복 테스트
- 폰 레시피와 안경 모드 간 조작 횟수 비교
- 오탐·미탐·지연·카메라 사용률 정리
- 시연 실패 시 사용할 녹화 영상 준비
- 실제 시연 3회 연속 성공 후 기능 동결

---

## 11. 기획 핵심

기획을 한 장으로 압축한 절이다. 각 항목의 근거와 상세는 해당 절을 가리킨다.

### 11.1 문제

> 레시피에는 "5분 볶으세요"라고 쓰여 있지만 실제 완료 시점은 화력·양·팬에 따라 달라진다. 사용자는 손에 물과 기름이 묻은 채 계속 폰을 확인해야 한다.

시간을 완료 조건으로 쓰면 양쪽으로 틀린다. 빨리 익었는데 기다리게 하거나, 덜 익었는데 넘어간다. 타이머는 이 둘을 구분하지 못하고, 스톡 AI는 대본을 읽을 뿐 사용자가 지금 어느 단계에 있는지를 보지 않는다 (§1.2).

### 11.2 그래서 무엇을 바꾸는가

> **시간은 완료 조건이 아니라 검사 시작 조건이다.**

명세서 전체가 이 문장에서 파생된다. 동작 원리는 §3.1, 검사 예약은 F3, 판정은 F4.

### 11.3 설계 결정 세 가지

| 결정 | 근거 | 상세 |
|---|---|---|
| 연속 촬영하지 않고 **검사 시점에만 2~3초** | 배터리·발열, 그리고 1분 이상 스트림 중 캡처 시 간헐적 충돌 | §2 · F3 |
| 한 장이 아니라 **시작·현재 두 장을 비교** | "반투명하다"는 절대 상태가 아니라 상대 개념이다. 기준점 없이 물으면 답이 흔들린다 | F4-7 · [`CONTRACT.md`](CONTRACT.md) §3.2 |
| 애매하면 **CANNOT_TELL** | 정확도와 오탐이 충돌하면 오탐을 버린다. 놓친 완료는 한 번 더 보면 되지만 잘못된 DONE은 되돌릴 수 없다 | §5.1 · §13 |

가운데 항목은 **아직 가설이다.** 검증 책임은 3번에게 있고 판정 기준은 §12 #3이다.

### 11.4 범위를 좁힌 근거

"딱 알맞게 익었는가"는 사람도 판단이 갈리고, LLM은 **확신 있게 틀린 답을 내놓는다.** 그래서 정도·질감 판정(C등급)을 MVP에서 빼고, 존재·식별(A등급) 중심에 색상 변화(B등급) 대표 단계 하나만 넣는다 (§5.1).

기능을 줄인 것이 아니라 **실패 모드를 고른 것**이다. "네, 딱 좋습니다" 하고 태우는 것이 이 서비스의 최악의 실패다.

### 11.5 무엇으로 성공을 판정하는가

> 연속 촬영 없이 간헐적인 1인칭 시각 정보만으로 실제 작업 단계 전환을 신뢰성 있게 판단할 수 있는가 (§1.3)

아이디어의 신기함이 아니라 **손을 덜 쓰면서도 잘못 다음 단계로 넘어가지 않는가**를 잰다. 측정 항목과 목표치는 §12, 미달 시 대응은 §13.

한 줄로 줄이면 — **잘못된 자동 진행 0회.**

요리는 이 구조를 검증하기 위한 대표 시나리오이며, 같은 구조를 조립·실험·정비로 확장할 수 있다 (§1.3 · §14).

---

## 12. 실측 항목

| # | 항목 | 방법 | 목표 |
|---|---|---|---|
| 1 | 반복 스트림 안정성 | `start→3초→capture→stop` 20회 | 19회 이상 성공 |
| 2 | 스트림 시작 지연 | 명령부터 첫 프레임까지 | 평균 2초 이내 목표 |
| 3 | 판정 정확도 | 실제 DAT 시작/현재 이미지 20쌍 | A등급 80% 이상, 잘못된 자동 진행 0회 |
| 4 | 왕복 지연시간 | 검사 시작→판정→음성 출력 | 3초 이내 목표 |
| 5 | 카메라 사용률 | 스트림 활성 시간/전체 세션 | 기록 후 15% 이하 목표 |
| 6 | 폰 마이크 | 조리대 1.5m, 후드 작동 중 20회 | 90% 이상 |
| 7 | 비교 사용성 | 폰 레시피와 안경 모드로 같은 작업 | 폰 조작 횟수 감소, 단계 누락 증가 없음 |

정확도 테스트는 임의의 고화질 사진이 아니라 **실제 DAT 스트림 또는 `capturePhoto()` 결과**를 사용한다. 전체 정확도뿐 아니라 잘못 완료로 판단한 비율, 완료를 놓친 비율, CANNOT_TELL 비율을 따로 계산한다.

---

## 13. 리스크

| 리스크 | 영향 | 대응 |
|---|---|---|
| 반복 스트림 시작이 느리거나 불안정 | 검사 지연 | 한 단계 동안만 2FPS 스트림 유지 후 단계 종료 시 중단 |
| 판정 정확도 미달 | 서비스 핵심 약화 | A등급으로 축소 → "확인해줘" 기반 수동 검사형으로 전환 |
| 잘못된 DONE | 위험한 자동 진행 | B등급 연속 DONE 2회, "아직" override, C등급 제외 |
| 네트워크·서버 실패 | 판정 중단 | 레시피 안내와 타이머는 로컬에서 계속 진행 |
| 카메라가 대상을 못 봄 | CANNOT_TELL 반복 | 검사 전 "팬을 봐주세요" 안내, 3회 후 수동 모드 |
| 폰 마이크 인식 실패 | 명령 불가 | 짧은 명령어만 사용하고 큰 화면 버튼 fallback 제공 |
| 배터리·발열 | 세션 중단 | 간헐적 스트림, 검사 간격 확대, 필요 시 해상도 하향 |
| 시연 중 실패 | 신뢰도 저하 | 대표 레시피 1개 고정, 시연 녹화본 준비, 실제 시연 3회 리허설 |
| 팀 등록 충돌 | 개발 지연 | 기기 담당 1명 고정, 나머지는 MockDeviceKit·테스트 이미지 사용 |

식품 안전이나 "완벽하게 익었다"는 판정은 하지 않는다. 본 서비스는 시각적으로 명확한 단계 전환을 보조하며, 불확실하면 반드시 CANNOT_TELL로 처리한다.

---

## 14. 확장 (범위 외)

- 영상 URL → 레시피 자동 변환
- 글래스 마이크(HFP) 전환으로 폰과 떨어진 환경에서도 음성 명령
- C등급 판정을 위한 데이터 수집 및 파인튜닝
- 조리 기록 기반 개인화
- 같은 판정 엔진을 실험·정비·조립 등 절차 기반 작업에 적용
- 다른 작업 1종을 추가해 범용 물리 작업 추적 엔진으로 검증
